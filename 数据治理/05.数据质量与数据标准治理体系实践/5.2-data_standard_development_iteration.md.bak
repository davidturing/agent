# 5.2 数据标准体系的构建、推广与迭代

> **摘要**: 数据标准是数据世界的“普通话”。本节借鉴华为与阿里的实战经验，详解业务、技术、管理三维标准的构建方法，并剖析如何解决“标准定完没人用”的落地顽疾。

---

## 5.2.1 华为数据标准三维分类

华为将数据标准划分为三个维度，缺一不可。

### 1. 业务标准 (Business Standards)
*   **定义**: 明确数据的业务含义，解决“歧义”。
*   **内容**:
        *   **业务术语**: 什么是“签约合同金额”？（含税还是不含税？汇率按哪天的算？）
        *   **命名规范**: 统一叫 `Customer` 还是 `Client`？

### 2. 技术标准 (Technical Standards)
*   **定义**: 明确数据在计算机里的存储形式，解决“异构”。
*   **内容**:
        *   **数据类型**: 金额统一用 `DECIMAL(18,2)`。
        *   **编码规范**: 性别字段统一用 `CHAR(1)`，取值 `'M'/'F'`。

### 3. 管理标准 (Management Standards)
*   **定义**: 明确数据的归属和权限，解决“责权”。
*   **内容**:
        *   **数据 Owner**: 谁负责？
        *   **安全等级**: L1-L4 级。
        *   **保留期限**: 存几年？

---

## 5.2.2 跨部门协同：解决“两张皮”问题

最大的痛点是：IT 部门起草了一堆标准，业务部门根本不知道，也不照做。这就叫“标准与业务两张皮”。

### 华为经验：业务挂帅，IT 落地
*   **制定**: 标准必须由 **“数据 Owner” (业务部门老大)** 签字发布。IT 只是起草的秘书。
*   **发布**: 通过红头文件发布，纳入公司流程体系 (IPD/ISC)。不符合标准的需求，IT 有权拒绝开发。

### 阿里经验：工具约束，强行对齐
*   **模型设计中心**: 在 DataWorks 里，你建表时必须选择标准词根。
        *   你想建个字段叫 `gongzi` (工资)？系统报错：没有这个标准词根，请使用 `salary`。
        *   **强制**: 不符合标准的表，建不出来 (DDL 被拦截)。

---

## 5.2.3 落地的灵活性：刚性与弹性的平衡

过度的标准化会扼杀创新。特别是新业务探索期，如果非要等标准定好才能跑数，业务早就黄了。

### 策略：分级管理，动态迭代

1.  **核心数据 (Core Data)**:
        *   如财务、人力、核心交易数据。
        *   **强管控**: 必须 100% 符合标准。

2.  **探索性数据 (Discovery Data)**:
        *   如临时活动页面的埋点、新上的试验性业务。
        *   **弱管控**: 允许先没有标准，"先污染后治理"。
        *   **迭代**: 当新业务稳定后，再由数据管家介入，将其纳入标准体系。

### 试点推广路径 (Pilot Approach)
*   不要试图一次性统一全公司的标准。
*   **路径**: 选取一个痛点最痛的域（比如“客户主数据”） -> 集中攻坚 -> 看到成效（比如营销精准度提升） -> 推广到其他域。
